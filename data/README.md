# Data

The datasets used by Tego are released separately at:

https://anonymous.4open.science/r/Tego-derivation

This repository intentionally does not duplicate the released datasets.
Download the required CSV files from the dataset repository and pass their
local paths to `train.py` / `infer.py`.
